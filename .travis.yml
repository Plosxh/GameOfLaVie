language: python
python:
  - "3.6"
# command to install dependencies
install:
- pip install codecov
# command to run tests
before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3
  
script:
- coverage run Test.py

after_success:
- codecov
